name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Automaattestid
    runs-on: ubuntu-latest
    steps:
      - name: Lae kood alla
        uses: actions/checkout@v4

      - name: Paigalda Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Paigalda sõltuvused
        run: cd monolith && npm install

      - name: Käivita rakendus
        run: cd monolith && node src/server.js &

      - name: Oota kuni server käivitub
        run: sleep 3

      - name: Käivita testid
        run: cd monolith && node src/test.js
